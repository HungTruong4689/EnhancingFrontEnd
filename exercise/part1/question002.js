//What is a prototype chain

// create new type of objects based on existing ones
// inheritance prototype chain (properties and methods)
// prototype chain is a series of objects connected by the prototype property
// available through the prototype property of the constructor function Object.getPrototypeOf(object) or object.__proto__